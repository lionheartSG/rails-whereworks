<%= render "indexnavbar" %>

<div class="container">
  <h1><%= @listing.name %></h1>
  <p><%= @listing.description %></p>
  <div class="booking-form">
  <%= simple_form_for([@listing, @booking]) do |f| %>
          <%= f.input :reservation_startdatetime %>
          <%= f.input :reservation_enddatetime %>
          <%= f.input :quantity %>
          <%= f.submit %>
          <% end %>
  </div>
</div>



<%= render "footer" %>
